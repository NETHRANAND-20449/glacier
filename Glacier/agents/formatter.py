from typing import Dict

class FormatterAgent:
    def format_output(self, article: Dict) -> Dict:
        """
        Format the article, summary, and tags for output (JSON, dashboard, etc).
        Returns a formatted dict.
        """
        # TODO: Implement formatting logic
        return article