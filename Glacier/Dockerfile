FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8501 8000

CMD ["/bin/bash", "-c", "streamlit run ui/dashboard.py & uvicorn api.main:app --host 0.0.0.0 --port 8000"]